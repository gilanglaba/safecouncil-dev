# SafeCouncil Frontend Environment Variables
# In development: leave VITE_API_URL empty — Vite proxy handles /api/* routing to localhost:5000
# In production: set this to your backend URL
VITE_API_URL=
